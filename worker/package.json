{
  "name": "hobbily-ai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: Expo AI Assistant -> Gemini API (free tier) -> validated chat reply / activity JSON.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "~5.9.2",
    "wrangler": "^4.112.0"
  }
}
